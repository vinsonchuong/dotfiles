#!/usr/bin/env bash
set -o errexit
set -o nounset
# set -o xtrace
set -o pipefail

/usr/bin/beet "${@}"

{
	LOCK_FILE=${HOME}/music/update-google-music-manager
	sleep 5
	touch "$LOCK_FILE"
	sleep 1
	rm -f "$LOCK_FILE"
} &
