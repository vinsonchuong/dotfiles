#!/usr/bin/env bash
set -o errexit
set -o nounset
# set -o xtrace
set -o pipefail

BIN=$(realpath "${BASH_SOURCE[0]}")
BIN_DIR=$(dirname "$BIN")
LINK_DIR="${HOME}/bin"

mkdir -p "${LINK_DIR}"
for file in $(ls "$BIN_DIR")
do
	ln -sf "${BIN_DIR}/${file}" "${LINK_DIR}"
done
